<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.pocketworld</groupId>
    <artifactId>pocketworld-parent</artifactId>
    <version>1.0.0-SNAPSHOT</version>
  </parent>

  <artifactId>pocketworld-slime</artifactId>
  <packaging>jar</packaging>

  <name>PocketWorld Slime Engine</name>
  <description>
    Standalone Slime Region Format read/write, compression, Anvil import/export, and storage-backend
    engine. Deliberately has no dependency on Bukkit/Paper/NMS so it can be built, tested, and reused
    independently of the plugin runtime.
  </description>

  <dependencies>
    <!-- NBT tree implementation: MIT licensed, not guaranteed present on every Paper build, so it is
         shaded into the final plugin jar by pocketworld-plugin rather than assumed on the classpath. -->
    <dependency>
      <groupId>net.kyori</groupId>
      <artifactId>adventure-nbt</artifactId>
      <version>4.23.0</version>
    </dependency>

    <!-- Zstd compression, the codec the Slime format specifies. BSD-2-Clause. -->
    <dependency>
      <groupId>com.github.luben</groupId>
      <artifactId>zstd-jni</artifactId>
      <version>1.5.7-3</version>
    </dependency>

    <!-- MySQL world-blob storage backend. -->
    <dependency>
      <groupId>com.zaxxer</groupId>
      <artifactId>HikariCP</artifactId>
      <version>6.3.0</version>
    </dependency>
    <dependency>
      <groupId>com.mysql</groupId>
      <artifactId>mysql-connector-j</artifactId>
      <version>9.3.0</version>
    </dependency>

    <!-- MongoDB world-blob storage backend (GridFS, so world size isn't bounded by the 16MB BSON
         document limit). -->
    <dependency>
      <groupId>org.mongodb</groupId>
      <artifactId>mongodb-driver-sync</artifactId>
      <version>5.5.1</version>
    </dependency>

    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>

    <!-- Spins up real, ephemeral MySQL/MongoDB containers for the storage-backend contract tests,
         so those tests exercise the real wire protocol/SQL dialect rather than an in-memory stand-in.
         Requires a local Docker daemon; tests skip (not fail) when one isn't available. -->
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>1.21.3</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>mysql</artifactId>
      <version>1.21.3</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>mongodb</artifactId>
      <version>1.21.3</version>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>
